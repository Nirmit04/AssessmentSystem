import { Route } from '@angular/router';
import { CreateScheduleComponent } from './create-schedule.component';
export const CreateScheduleRoutes: Route[] = [
    {
        path: '',
        component: CreateScheduleComponent
    }
];
